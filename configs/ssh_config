# File: $HOME/.ssh/config
#
# The following settings aim at keep ssh connections up longer
# and to avoid broken pipe errors
#
Host *
  ServerAliveInterval 30
  ServerAliveCountMax 5
  TCPKeepAlive yes

# Use IpV43 for all dren.mil hosts
Host *.dren.mil
  AddressFamily inet
